using System.Collections.Generic;

//DON'T MODIFY, AUTOGENERATED CODE HERE (25.02.2020 07:49)

namespace Ecs
{
    public partial class Entity
	{
		public Game.Look look { get; private set; }

        public bool hasLook { get { return look != null; } }

        public void setLook(UnityEngine.Vector3 direction)
        {
			if (look == null)
				look = pool.look.Get();

			look.direction = direction;

			react.On(look);
        }

		private void addListener(Listener<Game.Look> obj)
		{
			if (obj == null) return;

			react.Add(obj);

			if (look != null) obj.On(look);
		}

		public void unsetLook(bool needReact = true) 
		{
			remove(look, pool.look);
			look = null;

			if (needReact)
				react.On(look);
		}
	}

	public partial class EntityListener
	{
		private List<Listener<Game.Look>> onLook = new List<Listener<Game.Look>>();

		public void Add(Listener<Game.Look> obj)
		{
			onLook.Add(obj);
		}

		public void On(Game.Look look)
		{
			foreach(var obj in onLook) obj.On(look);
		}
	}
}